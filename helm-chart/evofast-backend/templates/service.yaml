apiVersion: v1
kind: Service
metadata:
  name: evofast-backend-service
spec:
  selector:
    app: evofast-backend
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
